import { ABAxis, LAB, RGB, LCH, Constraint, TargetColour, Range } from "@/types/colours";

export default function Trial({ startColour, targetColour, onSubmit }: {startColour: RGB, targetColour: string, onSubmit: ()=>void}) {
    // For abstraction of White and Hue trials generally.
    return (<></>)
}

